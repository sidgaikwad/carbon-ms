{
  "name": "@carbon/kv",
  "dependencies": {
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@carbon/logger": "*",
    "@carbon/tsconfig": "*",
    "@types/node": "^20.12.5",
    "dotenv": "16.3.1",
    "ioredis-mock": "^8.13.1",
    "rimraf": "^5.0.5",
    "tsup": "^6.5.0",
    "tsx": "^4.13.2",
    "typescript": "^5.2.2",
    "vitest": "^4.1.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "private": true,
  "scripts": {
    "clean": "rimraf .turbo node_modules dist",
    "dev": "tsup --watch",
    "lint": "biome lint --write ",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "sideEffects": false,
  "version": "*"
}
